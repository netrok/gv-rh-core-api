spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/bd_rh
    username: postgres
    password: riesenhammer
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate   # o 'update' si quieres que Hibernate cree/ajuste la tabla automáticamente
    show-sql: true
    properties:
      hibernate.format_sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration

server:
  port: 8080

logging:
  level:
    root: INFO
    org.springframework.web: INFO
    org.hibernate.SQL: DEBUG

springdoc:
  swagger-ui:
    path: /swagger-ui.html
